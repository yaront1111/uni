import React from 'react';
const navigation=['Today','Ask','Commitments','Decisions','Memory Inspector','Permissions'];
/** `current` is null on a screen reached from another one rather than from the
 * navigation, such as an answer's provenance. */
export function Navigation({current}:{current:'devices'|'sources'|'connectors'|'jobs'|'registry'|'merge-split'|'weekly-review'|'memory-inbox'|'approval-rules'|null}){
  return <nav aria-label="Main navigation"><a href="/" aria-current={current==='devices'?'page':undefined}>Devices</a><a href="/sources" aria-current={current==='sources'?'page':undefined}>Documents</a><a href="/connectors" aria-current={current==='connectors'?'page':undefined}>Connected sources</a><a href="/ops/jobs" aria-current={current==='jobs'?'page':undefined}>Jobs</a><a href="/ops/registry" aria-current={current==='registry'?'page':undefined}>Registry</a><a href="/memory/merge-split" aria-current={current==='merge-split'?'page':undefined}>Merge and split</a><a href="/weekly-review" aria-current={current==='weekly-review'?'page':undefined}>Weekly Review</a><a href="/memory/inbox" aria-current={current==='memory-inbox'?'page':undefined}>Memory Inbox</a><a href="/memory/approval-rules" aria-current={current==='approval-rules'?'page':undefined}>Learned approval rules</a>{navigation.map(label=><span key={label} aria-disabled="true" title="Available when this feature is delivered">{label}</span>)}</nav>;
}
