// Uai memory kernel. Modules are landed slice by slice by moe-next agents; every
// slice ships with its own vitest suite and is verified by the daemon before it
// is accepted. See prd.md (the product authority) and .moe-next/README.md.
export const UAI_KERNEL_VERSION = "0.1.0";
